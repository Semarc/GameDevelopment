//es sollen sich ja mindestens zwei objekte bewegen deshalb 
//können wir die beiden piekser vlt so hoch schieben dass sie dann durchs bild fallen wenn der spieler dort ist

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FallingPiekser : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
